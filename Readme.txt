To run the test, either run python pop_task.py, having set up and entered the virtualenv using requirements.txt.

Or use jupyter notebook (having set up and entered the virtualenv using requirements.txt)

There are a few pieces still in need of work, but in the interest of not spending a huge amount of time on them I have merely marked them out. Otherwise the solution works, though I was unclear as to whether hitting the edge of the world should result in bursting and have therefore assumed no. 

Also, it currently shows the last frame before play is struck for the first time. 
